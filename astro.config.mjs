// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  // GitHub Pages: https://<ユーザー名>.github.io/<リポジトリ名>/ で公開される想定
  site: 'https://serenlueur.github.io',
  base: '/rookie_fes_26a/',
});
